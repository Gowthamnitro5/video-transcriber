services:
  - type: web
    name: video-transcriber
    runtime: docker
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.9.0"
      - key: FLASK_ENV
        value: "production"
    disk:
      name: uploads
      mountPath: /app/uploads
      sizeGB: 1
    autoDeploy: true